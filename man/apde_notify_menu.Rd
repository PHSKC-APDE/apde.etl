% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notify.R
\name{apde_notify_menu}
\alias{apde_notify_menu}
\title{Launch APDE Notify Management Interface}
\usage{
apde_notify_menu()
}
\value{
A Shiny app object that launches the management interface
}
\description{
Launches an interactive Shiny web application for creating, editing, and
managing email notification templates and recipient lists. This is the primary
interface for setting up the notification system before using \code{\link[=apde_notify]{apde_notify()}}
to send emails. The app provides forms to create message templates with variable
placeholders, manage email addresses, and assign recipients to specific messages.
}
\details{
The Shiny app includes three main sections:
\itemize{
\item \strong{Message Management}: Create/edit email templates with subject, body,
and sender address. Supports glue syntax for variable substitution.
\item \strong{Recipient Lists}: Assign email addresses to specific message templates
using a multi-select interface.
\item \strong{Address Management}: Add, edit, or remove email addresses from the
system-wide address book.
}

All changes are saved directly to the HHSAW and are immediately available for
use with \code{\link[=apde_notify]{apde_notify()}}.
}
\note{
This function replaces the deprecated \code{apde_notify_menu_f()} function from the
\code{apde} package.
}
\examples{
 \dontrun{
  # Launch the notification management Shiny app
  apde_notify_menu()

  # Typical workflow:
  # 1. Run apde_notify_menu()
  # 2. Add email addresses to the system
  # 3. Create message templates
  # 4. Assign recipients to each message
  # 5. Use apde_notify() to send notifications
 }

}
\seealso{
\code{\link[=apde_notify]{apde_notify()}} for sending notifications using templates created here
}
