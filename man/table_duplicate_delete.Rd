% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_duplicate.R
\name{table_duplicate_delete}
\alias{table_duplicate_delete}
\title{Delete SQL tables matching a specified suffix pattern}
\usage{
table_duplicate_delete(conn, delete_table_suffix = "_dupe_table_to_delete")
}
\arguments{
\item{conn}{Name of the connection to the SQL database}

\item{delete_table_suffix}{String pattern to match table names for deletion.
Uses SQL LIKE pattern matching with wildcards. Default is
\code{"_dupe_table_to_delete"}.}
}
\value{
None (invisible). Tables are deleted from the database.
}
\description{
Identifies and deletes SQL tables whose names contain a specified suffix
pattern. Includes user confirmation before deletion to prevent accidental
data loss.
}
\details{
This function queries the INFORMATION_SCHEMA.TABLES to find tables whose names
contain the given suffix pattern using SQL LIKE matching (e.g., \verb{\%suffix\%}).
It presents a list of matching tables and requires user confirmation before
performing the deletion.

\strong{Warning:} This function permanently deletes tables. Always review the list
of tables to be deleted before confirming.
}
\note{
This function replaces the deprecated \code{table_duplicate_delete_f()} function from the
\code{apde} package.
}
\examples{
 \dontrun{
  # Setup connection
  conn <- create_db_connection("my_server", prod = FALSE)

  # Delete tables with default suffix
  table_duplicate_delete(conn)

  # Delete tables with custom suffix
  table_duplicate_delete(conn, "_backup_old")

  # Disconnect when done
  DBI::dbDisconnect(conn)
 }
}
\seealso{
\code{\link[=table_duplicate]{table_duplicate()}} for creating tables that may need cleanup
}
\author{
Jeremy Whitehurst, 2024-05-01
}
