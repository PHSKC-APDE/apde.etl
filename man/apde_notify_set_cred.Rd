% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/notify.R
\name{apde_notify_set_cred}
\alias{apde_notify_set_cred}
\title{Set Up Outlook Email Credentials for APDE Notify}
\usage{
apde_notify_set_cred()
}
\value{
None
}
\description{
Configures and stores Outlook SMTP credentials required for sending email
notifications. This function prompts for your King County email address and
password, then securely stores the credentials using the blastula package's
key management system. The stored credentials are automatically used by
\code{\link[=apde_notify]{apde_notify()}} when sending emails.
}
\details{
This function creates an interactive setup process:
\enumerate{
\item Prompts for email address (defaults to your system username + @kingcounty.gov)
\item Opens a secure password dialog for entering your email password
\item Stores credentials with SSL encryption for Outlook SMTP
\item Saves credentials with ID "outlook" for automatic retrieval
}

Credentials are stored locally and persist between R sessions. This function
is automatically called by \code{\link[=apde_notify]{apde_notify()}} if no credentials are
found, but can also be run manually to update or reset credentials.
}
\note{
This function requires interactive input and cannot be run in non-interactive
environments. Credentials are stored securely using the operating system's
credential management system.

This function replaces the deprecated \code{apde_notify_set_cred_f()} function from the
\code{apde} package.
}
\examples{
 \dontrun{
  # Set up email credentials manually (although usually called by apde_notify())
  apde_notify_set_cred()
 }

}
\seealso{
\code{\link[=apde_notify]{apde_notify()}} which automatically calls this function when needed
}
